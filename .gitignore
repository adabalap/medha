*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build/
/app/build/
/captures/
.externalNativeBuild/
.cxx/

# The Gradle wrapper is intentionally committed: it pins the build tool version
# so CI and every developer use the same Gradle. Do not let a broad *.jar rule
# swallow it.
!gradle/wrapper/gradle-wrapper.jar
